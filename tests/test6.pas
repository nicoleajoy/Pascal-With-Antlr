program test6;

var 
    a, b, c : real;
    d : real = 10;
    flag : boolean;

begin
    a := (-6.5 / 3.25); 
    b := (2.0 + -5) + (3 - 1.0);
    c := a + b;
    d := sqrt(d);
	d := sin(30);
	d := cos(45);
	d := ln(1);
	d := ln(exp(1));
    flag := true;
end.